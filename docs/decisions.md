# 技术决策记录（ADR）

> 每条记录一个「影响双方 / 难以回退」的决定。谁做决定谁写。格式：决定 / 背景 / 后果。

---

## ADR-001：第一阶段做 Web/PWA，后续用 Capacitor 封装移动端

- **日期**：2026-07-06 · **决定人**：Codex 提议，Claude 确认
- **决定**：先做 React + Vite + PWA 网页版（手机浏览器可用），验证后再用 Capacitor 封装成 iOS/安卓 App。
- **背景**：Owner 要的是手机 App，但原生开发成本高、上架慢、两个 AI 迭代不便。Web 先行成本最低、迭代最快。
- **后果**：早期不承担原生复杂度；曲谱阅读、MIDI 播放、搜索收藏都能用 Web 技术实现；后期实时麦克风纠错等能力再上原生模块。

## ADR-002：跟弹内容用 MusicXML + MIDI，不用 PDF

- **日期**：2026-07-06 · **决定人**：Claude 提出，Owner 拍板「要虫虫那种高亮效果」
- **决定**：跟弹 / 高亮 / 瀑布流的曲谱一律用**结构化乐谱数据（MusicXML 渲染 + MIDI 发声）**，PDF/图片仅用于「看原谱 / 下载 / 打印」。
- **背景**：PDF 本质是图片，程序无法知道每个音符的位置和时值，**无法实现「弹到哪高亮到哪」**——而这是对标虫虫的核心体验。OSMD 能把 MusicXML 渲染成可高亮、带光标的谱面。
- **后果**：
  - 内容准备必须优先产出 MusicXML（Codex 的内容策略需同步）。
  - 渲染库锁定 OpenSheetMusicDisplay（OSMD）。
  - 跟弹引擎 = OSMD 光标步进 + Tone.js 按时值发声 + 键盘高亮，三者用同一份乐谱数据驱动。

## ADR-003：V0.1 = 可点击原型，不接后端

- **日期**：2026-07-06 · **决定人**：Codex 提议，Claude 确认
- **决定**：第一版只做前端可点击原型，6 个页面 + 10~20 首 mock 数据，无登录、无真实后端。
- **背景**：先验证产品体验和页面流程，避免过早投入后端 / 账号体系。
- **后果**：数据放 `app/src/data/`；曲谱文件放 `app/src/assets/scores/`；后端（Supabase 或 Node/NestJS + PostgreSQL）留到 V0.2。
